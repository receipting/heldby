# heldby — build plan

See the repo README for the framework. This is the slice plan.

- [x] **0 · Catalogue + skeleton** — schema with per-ecosystem names, 111 rules, loader,
      validator, digests, registry resolver, grow-only gate, nightly freshness Action.
- [ ] **1 · `heldby scan`** — TS/JS textual matching, Python via stdlib `ast`, config
      surfaces, gateway-absence detection, `--json` schema v0 with honest `skipped`.
- [ ] **2 · The skill** — `SKILL.md`, lazy references, classifier and verifier agents,
      emits `ai-register.md` + `.json`.
- [ ] **3 · Adopt** — `adopt` writes the vendored declaration and the typed wrapper;
      `lint` (three bypass routes, ratcheted); `emit --check`.
- [ ] **4 · README + diagrams** — three SVGs extracted from the framework deck.
- [ ] **5 · Evaluation** — cold-repo runs and a scored estate eval.
